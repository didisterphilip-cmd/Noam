<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/gate_background"
    android:orientation="vertical"
    android:paddingHorizontal="28dp"
    android:paddingTop="64dp"
    android:paddingBottom="36dp">

    <!-- Which app is being opened -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/appIcon"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:contentDescription="@string/app_icon" />

        <TextView
            android:id="@+id/breathText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:letterSpacing="0.12"
            android:text="@string/breath_inhale"
            android:textColor="@color/gate_accent"
            android:textSize="14sp"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- The ten seconds: a line sweeping up and back down, with the count on top -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="8dp"
        android:layout_weight="1">

        <com.noam.gate.SweepLineView
            android:id="@+id/sweepLine"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <TextView
            android:id="@+id/countdownText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:alpha="0.9"
            android:fontFamily="sans-serif-light"
            android:textColor="@color/gate_text"
            android:textSize="80sp"
            tools:text="10" />
    </FrameLayout>

    <!--
        The two buttons. They keep their space from the start (invisible rather
        than gone) so nothing jumps when the countdown ends.
    -->
    <LinearLayout
        android:id="@+id/decisionGroup"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        android:visibility="invisible">

        <!-- What the last day looked like, in the same reveal as the buttons -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="20dp"
            android:gravity="center_horizontal"
            android:orientation="vertical">

            <TextView
                android:id="@+id/statLastUsed"
                style="@style/GateStat"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />

            <TextView
                android:id="@+id/statTried"
                style="@style/GateStat"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />

            <TextView
                android:id="@+id/statDeclined"
                style="@style/GateStat"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />
        </LinearLayout>

        <Button
            android:id="@+id/buttonStop"
            style="@style/Widget.Material3.Button"
            android:layout_width="match_parent"
            android:layout_height="104dp"
            android:backgroundTint="@color/gate_accent"
            android:paddingHorizontal="16dp"
            android:text="@string/gate_stop"
            android:textColor="@color/gate_on_accent"
            android:textSize="21sp"
            android:textStyle="bold"
            app:cornerRadius="22dp" />

        <Button
            android:id="@+id/buttonContinue"
            style="@style/Widget.Material3.Button.TextButton"
            android:layout_width="wrap_content"
            android:layout_height="44dp"
            android:layout_marginTop="12dp"
            android:alpha="0.6"
            android:text="@string/gate_continue"
            android:textColor="@color/gate_text_muted"
            android:textSize="13sp" />
    </LinearLayout>
</LinearLayout>
